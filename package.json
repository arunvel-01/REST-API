{
  "name": "rest-api",
  "version": "1.0.0",
  "description": "This project implements a basic API for managing articles. It utilizes Node.js and Express for server-side operations, along with Mongoose for database interactions with MongoDB. The API supports operations like creating, retrieving, and deleting articles.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ARUNVEL",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "mongoose": "^7.5.0"
  }
}
